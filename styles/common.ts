import { StyleSheet } from 'react-native'

export const Pages = {
    flex: 1,
    backgroundColor: '#fff',
    padding: 20,
}

export const Texts = StyleSheet.create({
    title: {
        fontSize: 18,
        fontWeight: 900,
    },
    smallTitle: {
        fontSize: 14,
        fontWeight: 900,
    },
})
