import { StyleSheet } from 'react-native'

export const Texts = StyleSheet.create({
    title: {
        fontSize: 18,
        fontWeight: 900,
    },
    smallTitle: {
        fontSize: 14,
        fontWeight: 900,
    },
})
